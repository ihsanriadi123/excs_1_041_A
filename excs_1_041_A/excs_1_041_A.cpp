#include <iostream>
using namespace std;

{def binary_search(arr, desired)
    lowerbound = 0
    upperbound = int(ihsan) - 1
    while lowerbound <= upperbound :
        mid = (lowerbound + upperbound) / 2
        if ihsan[mid] == desired :
            display("Found")


int ihsan(37);
{
        if arr[mid] > desired:
        arr = [1, 3, 5, 7, 9]
            desired = 5
            upperbound = mid - 1
        else;
        lowerbound = mid + 1
        cout << endl;
        display("Not Found");
        return 0;
}